1-create folder
2-go to folder
3-from cmd - git init
4- git status
5- git add "../.."
6- git commit -m "message"
7- git push
8- to check git log --> git log
9- undo the changes we added git reset "git log id"
10- restore the conten file git restore "./flie name"
11- git stash -- it is used keep file in background whenever need we can call it using git stash pop and want to clean it git stash clear
12- clone you git repo to project you greated git remote add origin "repo link"
13- push the changes to the git hub
14- creating barnch git branch branch-name
15- to use this branch use git checkout branch-name
16- to push the branch git push origin branch-name
17-merger to master git merge branch-name

/**********
if you want to use already exist thing
1- git cline "link"
2- git fetch -- fetch all the changes donre in main
3- git pull will pull all the changes done in particular branch or master